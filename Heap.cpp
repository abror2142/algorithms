#include <iostream>

using namespace std;

/*
    >>>> Heap <<<<
    Operations:
        -- insert(key)
        -- extract()
        -- search(key)
        -- delete(key)
        -- getMin()
        -- increase/decreas(key)
*/

template <typename T > class MinHeap
{
    
};

int main (int argc, char * argv[])
{


    return 0;
};